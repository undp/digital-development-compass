interface CountryNameAndAlpha {
  name: string;
  alpha2: string;
  alpha3: string;
}

interface ColumnSetting {
  key: string;
  name: string;
  visible: boolean;
}

interface DisplaySettings {
  showHeatmap: boolean;
  showConfidence: boolean;
}
