name: data
on:
  schedule:
    - cron: 0 0 13 * *
  workflow_dispatch: {}
  push:
    paths:
      - .github/workflows/flat.yml
      - ./process_xlsx.js
jobs:
  scheduled:
    runs-on: ubuntu-latest
    steps:
      - name: Setup deno
        uses: denoland/setup-deno@main
        with:
          deno-version: v1.x
      - name: Check out repo
        uses: actions/checkout@v2
      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/countries_ixp.csv
           downloaded_filename: data/countries_ixp.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://www.mobileconnectivityindex.com/widgets/connectivityIndex/excel/MCI_Data_2020.xlsx
           downloaded_filename: data/countries_mobile_connectivity.xlsx
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/countries_ixp.csv
           downloaded_filename: data/countries_ixp.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: blob:https://www.pch.net/e201c2a4-3b8b-4599-bb20-dfbba0077864
           downloaded_filename: data/countries_ixp_2.svg
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://freedomhouse.org/sites/default/files/2020-10/FOTN_2020_Key_Internet_Controls_Updated.xlsx
           downloaded_filename: data/number_key_internet_controls_employed.xlsx
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://development-data-hub-s3-public.s3.amazonaws.com/ddhfiles/94586/wb_id4d_dataset_2018_0.xlsx
           downloaded_filename: data/id4d_nid.xlsx
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://development-data-hub-s3-public.s3.amazonaws.com/ddhfiles/94586/wb_id4d_dataset_2018_0.xlsx
           downloaded_filename: data/id4d_services.xlsx
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://opencellid.org/ocid/downloads?token=pk.6f7a8f2cfa2bb43ea72428ed4a4b4f47&type=full&file=cell_towers.csv.gz
           downloaded_filename: data/cell_towers_geolocation.csv.gz
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://www.mobileconnectivityindex.com/widgets/connectivityIndex/excel/MCI_Data_2020.xlsx
           downloaded_filename: data/%_population_internet_coverage_2g.xlsx
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://www.mobileconnectivityindex.com/widgets/connectivityIndex/excel/MCI_Data_2020.xlsx
           downloaded_filename: data/%_population_internet_coverage_3g.xlsx
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://www.mobileconnectivityindex.com/widgets/connectivityIndex/excel/MCI_Data_2020.xlsx
           downloaded_filename: data/%_population_internet_coverage_4g.xlsx
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://www.mobileconnectivityindex.com/widgets/connectivityIndex/excel/MCI_Data_2020.xlsx
           downloaded_filename: data/%_population_internet_coverage_5g.xlsx
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://api.worldbank.org/v2/en/indicator/EG.ELC.ACCS.ZS?downloadformat=excel
           downloaded_filename: data/%_population_electricity_coverage.xls
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: http://www3.weforum.org/docs/WEF_GCI_4.0_2019_Dataset.xlsx
           downloaded_filename: data/elect_supply_quality.xlsx
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://www.mobileconnectivityindex.com/widgets/connectivityIndex/excel/MCI_Data_2020.xlsx
           downloaded_filename: data/spectrum_allocated_mobile_providers.xlsx
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://www.itu.int/en/ITU-D/Statistics/Documents/statistics/2021/PercentIndividualsUsingInternet.xlsx
           downloaded_filename: data/%_individual_internet_usage.xlsx
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://www.itu.int/en/ITU-D/Statistics/Documents/statistics/2020/MobileCellularSubscriptions_2000-2019.xlsx
           downloaded_filename: data/%_mobile_cell_sub.xlsx
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/fixed_bdbd_spd_dl_ul.csv
           downloaded_filename: data/fixed_bdbd_spd_dl_ul.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/fixed_broadband_speed_download_upload_latency.yaml
           downloaded_filename: data/fixed_broadband_speed_download_upload_latency.yaml
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/countries_lpi.csv
           downloaded_filename: data/countries_lpi.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: http://www3.weforum.org/docs/WEF_GCI_4.0_2019_Dataset.xlsx
           downloaded_filename: data/elect_supply_quality.xlsx
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/elect_dist_grid_map.csv
           downloaded_filename: data/elect_dist_grid_map.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://www.itu.int/en/ITU-D/Statistics/Documents/statistics/2021/PercentIndividualsUsingInternet.xlsx
           downloaded_filename: data/%_individual_internet_usage.xlsx
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://www.mobileconnectivityindex.com/widgets/connectivityIndex/excel/MCI_Data_2020.xlsx
           downloaded_filename: data/tax_%_mobile_ownership.xlsx
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://development-data-hub-s3-public.s3.amazonaws.com/ddhfiles/1202896/public_use-talent-migration.xlsx
           downloaded_filename: data/migration_industry.xlsx
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://development-data-hub-s3-public.s3.amazonaws.com/ddhfiles/1202896/public_use-talent-migration.xlsx
           downloaded_filename: data/migration_skill.xlsx
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://development-data-hub-s3-public.s3.amazonaws.com/ddhfiles/1202896/public_use-talent-migration.xlsx
           downloaded_filename: data/migration_country.xlsx
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://stats.oecd.org/SDMX-JSON/data/PATS_COOP/EPO_A+PCT_A+USPTO_A.FOR_APP+FOR_APP_S+FOR_INV+FOR_INV_S+CO_INV+CO_INV_S.AUS+CAN+JPN+NLD+USA+EU27+EU28+WLD.TOTAL+WRD+JPN+USA+EU27+EU28.PRIORITY+APPLICATION/all?startTime=1999&endTime=2019&dimensionAtObservation=allDimensions&pid=0cb10255-1a67-42b2-a2f2-37ecfe3ebbd7
           downloaded_filename: data/international_co_inventions.json
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://unctad.org/system/files/information-document/DP.xlsx
           downloaded_filename: data/data_protect_privacy_legislation.xlsx
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://tcdata360-backend.worldbank.org/api/v1/datasets/53/dump.csv
           downloaded_filename: data/venture_cap_avail.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://www.doingbusiness.org/content/dam/doingBusiness/excel/Historical-data---complete-data-with-scores.xlsx
           downloaded_filename: data/doing_bus_idx.xlsx
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/legal_rights_strength.csv
           downloaded_filename: data/legal_rights_strength.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/ease_doing_bus.csv
           downloaded_filename: data/ease_doing_bus.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/time_start_bus.csv
           downloaded_filename: data/time_start_bus.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://dashboards.sdgindex.org/static/downloads/files/SDR%202021%20-%20Database.xlsx
           downloaded_filename: data/sdg_data.zip
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: http://hdr.undp.org/sites/default/files/2020_statistical_annex_table_1.xlsx
           downloaded_filename: data/undp_human_developmnt.xlsx
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://data.humdata.org/dataset/e9988552-74e4-4ff4-943f-c782ac8bca87/resource/a9e327cc-d63f-4e61-b13d-d1968ee018bf/download/county_country_aug2020.tsv
           downloaded_filename: data/fb_social_connectedness.tsv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://databank.worldbank.org/data/download/FINDEX_EXCEL.zip
           downloaded_filename: data/financial_inclusiveness.zip
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://api.worldbank.org/v2/en/indicator/IT.NET.USER.ZS?downloadformat=excel
           downloaded_filename: data/intenet_usage.xlsx
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://index.okfn.org/api/entries.csv
           downloaded_filename: data/open_data_idx.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://www.mobileconnectivityindex.com/widgets/connectivityIndex/excel/MCI_Data_2020.xlsx
           downloaded_filename: data/gender_gap_in_mobile_usage.xlsx
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://www.mobileconnectivityindex.com/widgets/connectivityIndex/excel/MCI_Data_2020.xlsx
           downloaded_filename: data/tax_%_mobile_ownership.xlsx
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://globalfindex.worldbank.org/sites/globalfindex/files/2018-08/Global%20Findex%20Database.xlsx
           downloaded_filename: data/%_population_digital_financial_services.xlsx
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://unstats.un.org/unsd/demographic-social/products/worldswomen/annex_tables/List%20of%20countries%20-%20Table%209.xls
           downloaded_filename: data/list_of_countries.xlsx
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://www.doingbusiness.org/content/dam/doingBusiness/excel/Historical-data---complete-data-with-scores.xlsx
           downloaded_filename: data/doing_bus_idx.xlsx
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://api.worldbank.org/v2/en/indicator/IT.NET.USER.ZS?downloadformat=excel
           downloaded_filename: data/%_population_using_internet.xls
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: http://hdr.undp.org/sites/default/files/2020_statistical_annex_all.xlsx
           downloaded_filename: data/undp_human_developmnt.xlsx
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://data.humdata.org/dataset/e9988552-74e4-4ff4-943f-c782ac8bca87/resource/a9e327cc-d63f-4e61-b13d-d1968ee018bf/download/county_country_aug2020.tsv
           downloaded_filename: data/fb_social_connectedness.tsv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://development-data-hub-s3-public.s3.amazonaws.com/ddhfiles/140602/country_level_data_0.csv
           downloaded_filename: data/ewaste_per_inhabitant.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://happiness-report.s3.amazonaws.com/2021/DataForFigure2.1WHR2021C2.xls
           downloaded_filename: data/global_wellbeing_initiative .xls
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://globalfindex.worldbank.org/sites/globalfindex/files/databank/Global%20Findex%20Database.xlsx
           downloaded_filename: data/financial_inclusiveness .xlsx
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://databank.worldbank.org/AjaxDownload/FileDownloadHandler.ashx?filename=364c4202-f6cd-4bcc-a02a-7f6e35e6b92a.xlsx&filetype=EXCEL&language=en&displayfile=Data_Extract_From_World_Development_Indicators.xlsx
           downloaded_filename: data/intenet_usage.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://index.okfn.org/api/datasets.json
           downloaded_filename: data/open_data_idx.json
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/spectrum_allocated_mobile_providers.xlsx
           downloaded_filename: data/spectrum_allocated_mobile_providers.xlsx
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://globalfindex.worldbank.org/sites/globalfindex/files/2018-08/Global%20Findex%20Database.xlsx
           downloaded_filename: data/%_population_digital_financial_services.xlsx
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/ict_goods.csv
           downloaded_filename: data/ict_goods.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/ict_services.csv
           downloaded_filename: data/ict_services.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://unstats.un.org/unsd/demographic-social/products/worldswomen/annex_tables/List%20of%20countries%20-%20Table%209.xls
           downloaded_filename: data/list_of_countries.xls
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: http://hdr.undp.org/sites/default/files/2020_statistical_annex_all.xlsx
           downloaded_filename: data/countries_hdi.xlsx
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/mobile_coverage.csv
           downloaded_filename: data/mobile_coverage.xls
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/mobile_broadband_pricing.csv
           downloaded_filename: data/mobile_broadband_pricing.csv 
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/mob_dev_pen.csv
           downloaded_filename: data/mob_dev_pen.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://www.mobileconnectivityindex.com/widgets/connectivityIndex/excel/MCI_Data_2020.xlsx
           downloaded_filename: data/gender_gap_in_mobile_usage.xlsx
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/ease_doing_bus.csv
           downloaded_filename: data/ease_doing_bus.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/time_start_bus.csv
           downloaded_filename: data/time_start_bus.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/venture_cap_avail.xlsx
           downloaded_filename: data/venture_cap_avail.xlsx
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/b2c_ecommerse_idx.csv
           downloaded_filename: data/b2c_ecommerse_idx.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/pop_dens_map.csv
           downloaded_filename: data/pop_dens_map.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://lpi.worldbank.org/sites/default/files/International_LPI_from_2007_to_2018.xlsx
           downloaded_filename: data/countries_lpi.xlsx
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/countries_gci.xlsx
           downloaded_filename: data/countries_gci.xls
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/gov_encrypted_traffic.csv
           downloaded_filename: data/gov_encrypted_traffic.zip
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://unctad.org/system/files/information-document/DP.xlsx
           downloaded_filename: data/glob_data_priv_law.xlsx
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/countries_mobile_connectivity.xlsx
           downloaded_filename: data/countries_mobile_connectivity.xlsx
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://www.schneier.com/cryptography/paperfiles/worldwide-encryption-product-survey-data.xls
           downloaded_filename: data/encrytion_data.xlsx
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://worldjusticeproject.org/sites/default/files/documents/FINAL_2020_wjp_rule_of_law_index_HISTORICAL_DATA_FILE.xlsx
           downloaded_filename: data/rli.xlsx
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://api.worldbank.org/v2/en/indicator/HD.HCI.OVRL?downloadformat=csv
           downloaded_filename: data/countries_hci.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/sci.csv
           downloaded_filename: data/sci.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/top_sites.csv
           downloaded_filename: data/top_sites.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/google_trends%20.xlsx
           downloaded_filename: data/google_trends.xlsx
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/youtube_searches.csv
           downloaded_filename: data/youtube_searches.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://www.itu.int/en/ITU-D/Statistics/Documents/statistics/2020/MobileCellularSubscriptions_2000-2019.xlsx
           downloaded_filename: data/%_mobile_cell_sub.xlsx
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://energydata.info/dataset/12c85425-14be-430b-ad72-c68804c49e8e/resource/dcda6530-8d2c-436e-9d1d-1e2809ad303e/download/enerdata_energy_statistical_yearbook_2018.xlsx
           downloaded_filename: data/electricity_yearbook.xlsx
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://www.itu.int/en/ITU-D/Statistics/Documents/facts/ITU_regional_global_Key_ICT_indicator_aggregates_Nov_2020.xlsx
           downloaded_filename: data/tii_countries.xlsx
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/legal_rights_strength.csv
           downloaded_filename: data/legal_rights_strength.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/postal_coverage.csv
           downloaded_filename: data/postal_coverage.json
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://nces.ed.gov/programs/digest/d18/tables/xls/tabn218.10.xls
           downloaded_filename: data/schools_w_internet.xls
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/dice_export_global_cybersecurity_index.xlsx
           downloaded_filename: data/dice_export_global_cybersecurity_index.xlsx
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/ecosystem.csv
           downloaded_filename: data/ecosystem.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://api.worldbank.org/v2/en/indicator/EN.POP.DNST?downloadformat=excel
           downloaded_filename: data/population_density.xls
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://api.worldbank.org/v2/en/indicator/IT.NET.BBND.P2?downloadformat=excel
           downloaded_filename: data/fixed_broadband_density.xls
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://dashboards.sdgindex.org/static/downloads/files/SDR%202021%20-%20Database.xlsx
           downloaded_filename: data/sustainability_index.xls
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://api.worldbank.org/v2/en/indicator/GB.XPD.RSDV.GD.ZS?downloadformat=excel
           downloaded_filename: data/RD_Percentage_GDP.xls
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://goingdigital.oecd.org/api/zip?destinations=https%3A%2F%2Fstats.oecd.org%2FSDMX-JSON%2Fdata%2FGD_BREAKDOWNS_1%2F5-1-2..ICT_TTL_GDP%2BICT_EQP_GDP%2BICT_CMP_GDP%2BICT_TLC_GDP%2BICT_SFW_GDP%2BIPP_TTL_GDP%2BIPP_RD_GDP%2BICT_TTL_GFCF%2BGFCF_TTL_GDP..%2Fall%3FcontentType%3Dcsv%26detail%3Dlabel
           downloaded_filename: data/ICT_Investment.zip
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://www.itu.int/en/ITU-D/Statistics/Documents/statistics/2021/July/IndividualsUsingInternetByGender.xlsx
           downloaded_filename: data/gender_gap_internet_usage.xlsx
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://api.worldbank.org/v2/en/indicator/IT.CEL.SETS.P2?downloadformat=excel
           downloaded_filename: data/%_mobile_subscription.xls
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://stats.oecd.org/modalexports.aspx?exporttype=excel&FirstDataPointIndexPerPage=&SubSessionId=9cd053e5-f9bc-488c-857a-21aaf62abbec&Random=0.4350978803289305
           downloaded_filename: data/start_up_investment.xls
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/SDG 4.4_Digital_literacy_data.csv
           downloaded_filename: data/SDG 4.4_Digital_literacy_data.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://api.worldbank.org/v2/en/indicator/IT.NET.USER.ZS?downloadformat=excel
           downloaded_filename: data/%_population_access_internet.xls
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://development-data-hub-s3-public.s3.amazonaws.com/ddhfiles/1232201/wbg_govtech-dataset_dec2020.xlsx
           downloaded_filename: data/Egov_strategy.xlsx
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/digital_public_service_use.csv
           downloaded_filename: data/digital_public_service_use.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/angel_investment.csv
           downloaded_filename: data/angel_investment.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/banking_sector_size.csv
           downloaded_filename: data/banking_sector_size.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/startup_eco_size.csv
           downloaded_filename: data/startup_eco_size.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/mobile_speed.csv
           downloaded_filename: data/mobile_speed.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/gender_gaps.csv
           downloaded_filename: data/gender_gaps.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/prevalance_gig_economy.csv
           downloaded_filename: data/prevalance_gig_economy.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/size_digital_economy.csv
           downloaded_filename: data/size_digital_economy.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/automation_led_unemployment.csv
           downloaded_filename: data/automation_led_unemployment.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/not_buying_online_concern_about_returning.csv
           downloaded_filename: data/not_buying_online_concern_about_returning.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/not_buying_online_concern_about_security.csv
           downloaded_filename: data/not_buying_online_concern_about_security.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/ICT_proportion.csv
           downloaded_filename: data/ICT_proportion.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/network_readiness_index.csv
           downloaded_filename: data/network_readiness_index.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/happiness_score.csv
           downloaded_filename: data/happiness_score.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/%_population_interacting_public_officials.csv
           downloaded_filename: data/%_population_interacting_public_officials.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/digital_payment_penetration.csv
           downloaded_filename: data/digital_payment_penetration.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/cloud_services.csv
           downloaded_filename: data/cloud_services.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/mobile_density.csv
           downloaded_filename: data/mobile_density.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://lpi.worldbank.org/sites/default/files/International_LPI_from_2007_to_2018.xlsx
           downloaded_filename: data/World Bank The Logistics Performance Index 2007 to 2018.xlsx
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/national_cybersecurity_index.csv
           downloaded_filename: data/national_cybersecurity_index.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/digital_platform_economy_index.csv
           downloaded_filename: data/digital_platform_economy_index.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/global_fintech_ranking.csv
           downloaded_filename: data/global_fintech_ranking.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/digital_skill_level.csv
           downloaded_filename: data/digital_skill_level.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/digital_work_ecosystem_size.csv
           downloaded_filename: data/digital_work_ecosystem_size.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/%_business_internet.csv
           downloaded_filename: data/%_business_internet.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/%_business_broadband.csv
           downloaded_filename: data/%_business_broadband.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/share_of_businesses_online_presence.csv
           downloaded_filename: data/share_of_businesses_online_presence.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/ease_of_finding_skilled_employees.csv
           downloaded_filename: data/ease_of_finding_skilled_employees.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/internet_use_female.csv
           downloaded_filename: data/internet_use_female.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/internet_use_male.csv
           downloaded_filename: data/internet_use_male.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/apps_in_national_language.csv
           downloaded_filename: data/apps_in_national_language.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/time_spent_online.csv
           downloaded_filename: data/time_spent_online.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/cryptocurrency_adoption.csv
           downloaded_filename: data/cryptocurrency_adoption.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/cyberbullying_rate.csv
           downloaded_filename: data/cyberbullying_rate.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/% of individuals buying online and frequency.csv
           downloaded_filename: data/% of individuals buying online and frequency.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/e-commerce_activity.csv
           downloaded_filename: data/e-commerce_activity.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/%_household_internet_access.csv
           downloaded_filename: data/%_household_internet_access.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/FB_users.csv
           downloaded_filename: data/FB_users.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/digital_payments_penetration.csv
           downloaded_filename: data/digital_payments_penetration.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/tech_hubs.csv
           downloaded_filename: data/tech_hubs.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/software_developer_ecosystem_size.csv
           downloaded_filename: data/software_developer_ecosystem_size.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/%_population_with_smartphones.csv
           downloaded_filename: data/%_population_with_smartphones.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/cyber_attacks.csv
           downloaded_filename: data/cyber_attacks.csv
           postprocess: ./process_raw_data.js

      - name: Fetch data
        uses: githubocto/flat@v2
        if: always()
        with:
           http_url: https://raw.githubusercontent.com/mbelinsky/public_development_data/main/e_government_index.csv
           downloaded_filename: data/e_government_index.csv
           postprocess: ./process_raw_data.js
